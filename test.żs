fn fibonacci(n) {
    if(n==0||n==1) {
        return n;
    }
    else(True) {
        return fibonacci(n - 1) + fibonacci(n - 2);
    }
}

fn some_loop ( x, y) {
    while(x < 10) {
        x++;
    }
    return (y + (x * 2));
}

fn main() {
    x = -10
    print(fibonacci(-x));
}
